# Stamp build metadata into release archives. `git archive` (used by the
# Release workflow) substitutes the $Format:...$ placeholders in
# build-info.json with the tagged commit's describe string and short hash,
# so every packaged build carries its own version. Live checkouts keep the
# literal placeholders and server.js falls back to querying git at startup.
build-info.json export-subst
