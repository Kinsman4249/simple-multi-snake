## Changelog

This file tracks real changes to this repository. For the rules on how entries
here should be written, see CHANGELOG_TEMPLATE.md.

#### Wormhole transition collision fix, mobile UI toggle, blue-shell equal-length rule (round twenty-five) -- v3.6.0
- Fixed a regression from the v3.4.0 per-segment wormhole threading: a snake could false-die from a wall or self collision PARTWAY through the transition, because while the body threads through (head at the landing, tail still draining through the entry) it is briefly discontinuous and collision ran against those transient positions. Fix: at fire, the snake is marked in-transit (`s.teleportDrain = body.length - 1`, decremented one per drained tail segment in applyMovementAndFood); while that counter is > 0 the head is EXEMPT from wall collision (it holds in place at a wall instead of dying) and from self collision (it threads through its own still-present body), and normal collision resumes the moment the last segment re-registers at the destination. Other snakes hitting the in-transit body are unaffected. New tests/pw_wormhole.js scenario watches every broadcast across the full drain and asserts the snake stays alive the whole way through a self-body wormhole.
- Mobile UI is now hidden by default behind a small toggle. On coarse-pointer devices ui.js adds a `.mobile-hidden` class to `<body>` at startup that removes every overlay -- status line, leaderboard, the fading top bar and its panels, and the touch PWR/BOOST buttons -- so nothing covers the board or eats touches; a small fixed round button (bottom-center, doesn't block the play area) flips the class to reveal/hide the full UI, pinning the top bar solidly visible while revealed. Swipe-to-steer keeps working while hidden, so play is uninterrupted. The class is the only state, so it resets to hidden on reload (session-scoped) as specified. Desktop is untouched (the toggle is created only when IS_TOUCH).
- Blue shell now respects an equal-length rule: it does NOT spawn when every living snake is the same length (the spawner's gate, alongside the existing two-player presence gate), and a shell already on the board fizzles -- acting like food (+1 growth), not launching -- if collected once all lengths have equalized. The check is evaluated at fire time from a settled-length snapshot taken before any head is unshifted that step (the collector's in-progress move would otherwise make it look one longer and wrongly fire), so it is continuously enforced rather than decided once at spawn. An already-launched projectile is unaffected ("too bad"). `allEqualLength()` deliberately returns false with fewer than two living snakes so it never overrides the maintainer-specced lone-survivor self-nuke. New pw_blueshell.js scenarios cover the equal-length fizzle and the equal-length spawn gate; `maxConcurrentFood` may now be set to 0 (food fully disabled) for deterministic lengths.

#### Average-length speed scaling, player-count food/powerup scaling, pixel bananas (round twenty-four) -- v3.5.0
- Global speed is now a function of the AVERAGE living-snake length (total combined length / number of living snakes), not the old wall-clock ramp, not total length, and not player count (server/state.js targetMoveIntervalMs). Equal averages give equal speed (one long + one short == two mediums); the curve is monotonic with an ease-out shape and SATURATES at move.lengthSaturation (default 40), so one long snake alone runs as fast as four long snakes. The ACTUAL interval eases toward the target with exponential smoothing over move.speedEaseMs (default 1000) via advanceGlobalSpeed(dt) each sim tick, so growth AND join/leave (which jump the average) glide instead of snapping. Speed is a single shared global value applied to every snake (S.moveIntervalMs), reset when the room empties. The old time-based move.rampIntervalSec/rampStepMs are gone (a leftover pair in an old config.json is ignored); startIntervalMs == minIntervalMs still pins a constant speed, which is how every timing-sensitive e2e test stays deterministic. New tests/pw_momentum.js already covered the boost momentum; the length model rides the existing suite (all tests pin move).
- Food and powerups scale with the player count in the room (occupied board slots), recomputed every sim tick so a join/leave adjusts immediately rather than leaving a stale count. Food: ceil(players/2) items (1-2:1, 3-4:2, 5-6:3, ...), clamped to the new maxConcurrentFood ceiling (default 8). Powerups: max(1, ceil(players/4)) (1-4:1, 5-8:2, ...), clamped to powerups.maxConcurrentPickups (now the CEILING, default raised 1->4). Food became an array (S.foods) end to end: the server places/tops-up/trims to target (ensureFoods, per-food rubberband bias preserved), the broadcast carries `foods` plus a `food` compat alias (foods[0]), the client predictor tracks a set of food keys and a single pending eat against any of them (predict.js), and both renderers draw every food with per-food predicted-eat hiding. Foods ride the wasm frame-input region (re-encoded per frame, MAX_FOODS 32) rather than the single snapshot food slot, so hiding stays per-frame; the old foodHidden flag is retired.
- Banana Trail is drawn as pixel-art bananas (a 5x5 grid-aligned yellow crescent with brown tips) instead of a flat yellow tint, so it reads distinctly from ice/poison trails. Identical bitmap + pixel-edge math (round(i*cell/5)) in render2d.js (BANANA_ART/drawBananaTile) and wasm/renderer.ts (bananaVal, trail loop) so the frozen-clock parity diff stays exact; the parity scene already lays banana trails and now also stages multiple foods.
- Leaderboard label "This machine" renamed to "Single PC" (index.html, README): the board is the uncontested / same-computer board, not tied to any specific machine -- the copy now says so rather than implying local-machine binding.
- maxPlayers default raised 4 -> 8 and made configurable at install time: install.sh prompts for it (MAX_PLAYERS env / last-value / default 8, validated 1-64), persists it like the port/simHz, and patches config.json via the existing Node one-liner -- no application-code edit needed. ~16 is the realistic ceiling on a Google Cloud e2-micro free-tier instance: network fan-out (each client's state payload grows with every player's segments, ~O(N^2) egress) and the 0.25 vCPU sustained baseline bind well before the 1 GB RAM does.

#### Momentum drift, drift dust, wormhole threading, session initials (round twenty-three) -- v3.4.0
- Momentum drift: speed is now per-snake STATE (s.rampProgress), not a function of the boost key. The hold-grace + ramp-up are unchanged, but releasing the key no longer snaps to base speed -- the momentum decays over new boost.decelMs (default 250, faster than the 400ms ramp-up but spanning multiple sim frames), driven by an updateMomentum step at the top of the sim loop that the accumulator multiplier, the broadcast moveMs/boost flag, and the wall-grace forfeiture all read. Drift eligibility follows the SPEED: any turn made while momentum >= new boost.driftThreshold (default 0.3) starts the body skid, scaled by the momentum at keypress -- including turns typed just after the key was released, which used to be plain turns the instant the key came up. Rapid alternating turns still chain skids (each turn re-arms driftDir from the previous heading, unchanged). The client mirrors the same decay math (BOOST_CFG.decelMs/driftThreshold via /api/config) so the predictor keeps flagging post-release turns as unpredictable exactly when the server will drift them. decelMs 0 restores the instant stop; sub-threshold momentum keeps the wall grace forfeited until fully decayed (still fast = still no mercy). New tests/pw_momentum.js: decay-through-intermediate-speeds on release, drift-after-release, and no-skid once decayed.
- Drift dust (replaces the old fixed 4-particle head scatter): every grid cell a body segment slides through during a drift gets one fading translucent dust particle -- spawn is snapshot-diff driven in main.js (a sliding player's segments' vacated cells, deduped, capped at 400, 300ms fade), and both renderers draw the same deterministic list (render2d drawDust; wasm gained a dust array in the frame-input region, MAX_DUST 256 stride 12, local-body pool offset moved 864 -> 3940 with the facade updated in lock step). Still gated by clientFx.slideDust. The parity scene stages a deterministic 8-particle dust trail so the pixel diff actually covers it.
- Wormhole teleports now THREAD per segment instead of relocating the whole body at once (the "visual snap" bug): on a successful rescue only the HEAD exits at the landing cell (body unshift + tail pop, zero net length change); because movement is head-unshift/tail-pop, each trailing segment then drains through the entry point one step at a time, preserving chain continuity across the transition -- self-body wormholes included. The old code rebuilt the entire body as a straight line behind the landing (which could even park segments out of bounds on a tight wall rescue). The landing scan still ignores the snake's own body -- the classic wall rescue steps back through its own head cell -- so a tight rescue can briefly self-overlap while the tail drains; transient and harmless (solid to others throughout; the head only dies if it re-enters a body cell on a later step). predict.js's teleport snap now applies to a body that is continuous except the head jump, so trailing segments keep interpolating. pw_wormhole.js now asserts threading (body[1] is the old head, never the cell behind the landing along the exit direction).
- Session-based score architecture (replaces the whole post-death initials prompt machinery): initials are entered UP FRONT -- P1 on the join gate (same overlay as the captcha; entry required, prefilled from localStorage) and P2 in a small prompt at its first WASD/add-player join -- bound to the seat for the session, sent via the new setInitials message, and a qualifying score is recorded server-side THE MOMENT the death/leave happens with the seat's bound initials (fallback "???" for a client that never sent any). A persistent INITIALS top-bar button overwrites either seat's initials any time, no refresh. DELETED: the PLAYING/FLUSHING/RESUMING state machine, pendingInitials/activeInitials, the "held" seat role (playerSeatCount/scoreMode/broadcast/status-bar all simplified), the askInitials/initials wire messages, initialsTimeoutMs, and the harness's auto-answer -- tests now pre-bind "BOT" at connect like the real client. pw_leaderboards.js gained a setInitials-overwrite scenario (records with the LATEST value, server-sanitized).
- Initials/P2-spawn input isolation (root cause of "typing initials spawns P2"): while ANY text entry is active (the join gate, the P2 initials prompt, the Change Initials panel, or focus in any input) the game's key handlers early-return entirely -- no movement, no boost, no seat requests; a listener gate, not a key remap (P1 initials via keyboard and P2-spawn-on-WASD both keep their keys). After any initials confirm, a 1.8s grace additionally ignores seat-REQUEST keypresses (movement of already-playing seats is unaffected) to absorb trailing keystrokes. Largely redundant with the session-initials rework (P1 entry now happens before the connection even exists) but closes the P2-prompt-during-play and Change-Initials-panel cases.
- config.json: boost.decelMs + boost.driftThreshold added; initialsTimeoutMs removed. Suite is now 14 e2e tests (pw_momentum added); parity re-verified after the wasm frame-layout change.

#### Split leaderboards, powerup framework docs + Hello World (round twenty-two) -- v3.3.0
- High scores are now TWO separate daily+allTime board pairs: "This machine" (one computer -- solo or couch co-op) and "Networked" (two or more computers), because racking up length alone and against live opponents are different skills (Phase 12, maintainer-confirmed split rule: connection count, not seat count -- couch co-op stays local). A run is classified at DEATH/LEAVE time by scoreMode() (connections owning at least one seat still in the game; spectator-only connections don't count) and the mode rides with the banked score through the initials flush, so a session changing shape later can never reclassify a finished run. Store is versioned ({version:2, modes:{local,networked}}, per-mode daily rollover); a flat pre-split highscores.json migrates in-memory into the local pair on first load (history to date was one-machine play) and persists in v2 shape on the next save -- the networked boards start fresh and fair. The hover panel shows both pairs with the session's CURRENT mode highlighted (server broadcasts `mode`), and updateLeaderboards tolerates a pre-split payload during a rolling deploy.
- New config key highscoresFile (default "highscores.json"): tests/helpers.js startServer now points every test server at its own temp .test-highscores-*.json (cleaned up by stopServer, gitignored) -- the whole e2e suite used to auto-submit BOT scores into the repo's REAL highscores.json on every run.
- Powerup framework documentation: powerups/README.md -- philosophy (one module per type), module anatomy with every hook signature, the registry, config integration, the self-describing POWERUP_INFO/legend flow, the pickup flow (auto-fire vs requiresActivation vs wormhole's charge), trails, the CRITICAL 3-place renderer color sync + wasm rebuild + parity step, the e2e test pattern (SNAKE_TEST_SPAWNS/SNAKE_TEST_HOOKS), and a copy-this checklist.
- powerups/helloWorld.js: the teaching template the doc points at -- ~20 lines, disabled by default (never appears in a real game or the legend), logs "Hello World!" via the new onActivate hook and runs a 1.02x speedMultiplier for 1s. Wired end-to-end (registry, config, defaults, renderer index 7 cyan in all three color tables + wasm rebuild + parity scene) with tests/pw_helloworld.js asserting auto-fire, the moveMs drop, and expiry.
- Two small framework generalizations shaken out by the example (both behavior-preserving for existing types): firePowerup now calls an optional module onActivate hook, and the speed-multiplier stack is discovered from the modules (SPEED_MULT_TYPES: every module exporting speedMultiplier, multiplied into both simLoop's accumulator and the broadcast moveMs) instead of hardcoding speedBoost+iceTrail -- a new speed-affecting powerup only writes the hook.
- Suite is now 13 e2e tests (pw_leaderboards, pw_helloworld added).

#### Server split, rubberbanding, Banana Trail, alternating held glow (round twenty-one) -- v3.2.0
- Split the monolithic server.js (1563 lines) into a server/ module tree with a ~300-line transport entry point, with NO behavior change (the full e2e suite gates both sides of the split): server/config.js (config/env resolution, tuning constants, powerup registry metadata), server/state.js (the single shared mutable state object S plus board/snake primitives), server/highscores.js, server/captcha.js, server/net.js (serialize-once broadcast fanout), server/lifecycle.js (seats/queue/co-op/idle/death/initials), server/sim.js (sim loop, movement, collisions, wormhole rescue, powerup runtime, blue shells, spawner). Dependency graph is acyclic; deno's CJS detection handles the tree unchanged. install.sh now syncs the server/ tree like powerups/ (a missing tree is MODULE_NOT_FOUND on boot) and patches the listening port in server/config.js, where the PORT const now lives; the README manual-install steps got the same additions.
- Rubberbanding (Phase 8, maintainer-specced, new `rubberband` config block, silent by design):
  - Food bias: placeFood() now prefers cells near the SHORTEST living snake's head -- a free cell within foodBias.radius (default 15, Chebyshev) is always accepted, farther cells only with probability 1/strength (default 3). Inert with one player or all-equal lengths, so solo play is byte-identical to the old uniform sampler. The rewrite also put a hard attempt cap + first-free-cell fallback + full-board scan on what was previously an UNBOUNDED rejection loop.
  - Shell pressure: when the leader is at least shellPressure.leadRatio (default 1.3 = 30% longer) times the second-longest living snake, blue shells spawn at spawnIntervalMs x intervalScale (default 0.5) and the spawn-type roll weights blueShell at typeWeight (default 4) vs 1. Runs strictly AFTER the two-player presence gate, so pressure can never re-introduce a shell the gate removed.
  - tests/pw_rubberband.js: statistical with wide margins -- biased vs control food-distance distributions via the placeFood test hook (near-trailer 31/100 vs 8/100 uniform in a reference run), pressure vs equal-length spawn-type streams (0.79 vs 0.25 blueShell fraction), and a lone-connection run asserting the gate always wins.
- Banana Trail (Phase 11, new powerup, type index 6): auto-fires on pickup and lays banana-peel tiles behind the layer (the trail-laying path is now data-driven off a TRAIL_TYPES set instead of a hardcoded ice/poison pair). Any snake whose head lands on a tile -- INCLUDING the layer -- gets its controls REVERSED for bananaTrail.invertDurationMs (default 4000ms; sim-clock based like the ice slow, refreshed by re-crossing). The inversion is applied in the dir handler BEFORE the reversal/duplicate validation, deliberately: left/right and up/down swap, while the "keep going forward" key inverts into a rejected reversal so slipping never adds a new self-death mode. Feedback: the status bar shows "⇄ CONTROLS REVERSED" for an affected local seat (the server broadcasts a per-player `inverted` flag), and the client predictor reuses the drift's `delayed` mechanism for presses made while inverted (the server will flip them, so the one-cell predictor renders the authoritative body instead of mispredicting). Renderer sync in all three places (render2d.js POWERUP_STYLE/TRAIL_STYLE, render.js POWERUP_TYPE_INDEX, wasm pickupColor/trailColor, banana yellow #fd4) + parity scene. tests/pw_bananatrail.js covers pickup auto-fire, tile laying, the crossing flag, the actual left-becomes-right reversal, expiry, and the layer slipping on their own peels -- staged with forced spawns + hooks, no greedy pathing. (Fun testing find: a snake that turns ALONG the striped row keeps re-slipping and stays inverted indefinitely -- real mechanic, the test steers off the row.)
- Held-powerup glow: when a snake has BOTH a held powerup and an armed wormhole charge, the glow now ALTERNATES between the two colors every 600ms (maintainer report: the wormhole rescue was invisible whenever speedBoost sat in the held slot). Same formula and list order in both renderers, keyed off the shared frame clock so the frozen-clock parity diff stays exact; the wasm player record grew a wormholeCharge field (stride 56 -> 64, writer/reader bumped together). The parity scene now actually exercises the glow (opts.heldGlow was never set there before -- the glow was previously outside the pixel-diff's coverage) including a both-ready player, and adds banana pickups/tiles. Parity after all of it: <=0.001% differing pixels.
- Suite is now 11 e2e tests (pw_rubberband, pw_bananatrail added); full suite green, parity green, live smoke green on both renderers.

#### Blue Shell ships on by default (round twenty, Phase 9) -- v3.1.0
- Blue Shell is enabled by default and now AUTO-FIRES the instant it is picked up, like every other non-speedBoost powerup (requiresActivation removed from powerups/blueShell.js). Targeting is unchanged and deliberate: the shell homes on whoever is CURRENTLY longest, re-targeted every step, including the player who collected it -- the leader's counterplay is to guard the shell pickup instead of only racing food, which is the strategic point of the powerup.
- New presence gate (the piece that made auto-fire safe to ship): playerSeatCount() counts connected PEOPLE still in the game -- seats in role "player", alive or dead awaiting respawn, plus seats parked "held" during an initials flush; spectators don't count. With fewer than two, the shell is filtered out of the spawn pool entirely, and a shell pickup that outlives the second player's DISCONNECT fizzles into the wasted-pickup +1 on collection. A merely-dead opponent still counts (their seat persists through spectatorPromoteDelayMs), so a lone survivor who grabs a shell fires it and self-nukes -- maintainer-specced: being the only one left alive is not supposed to make the shell safe.
- Fixed the wasted-pickup fallback granting +2 segments instead of the documented +1 (the blocked branch called growSegment AND set grew=true, so the unshifted head was kept too; food's +1 comes from grew alone). Now growSegment without grew: net +1, matching the comment and the food fallback it references.
- Test staging hooks (both inert in prod): SNAKE_TEST_SPAWNS entries accept an optional `len` (forces spawn length, so leader identity is deterministic at spawn), and a new SNAKE_TEST_HOOKS=1 env enables a `testHook` WS message -- spawnPickup (place a pickup of a chosen type at a chosen cell, i.e. directly in a snake's path so no steering is ever needed), grantPowerup (arm a wormhole charge / fire / hold a type on a slot), placeFood (re-roll food; used by upcoming rubberband distribution tests). tests/helpers.js grew a testHook() sugar.
- tests/pw_blueshell.js rewritten to be fully deterministic -- the historic staging blocker (greedy pathing walking snakes into walls/each other) is gone. Three snakes travel PARALLEL from forced spawns: leader (len 24, row 10) coasts into a hook-placed shell; victim (len 20, row 12, spawned 10 cells ahead so its body spans the impact column under any inter-connect lag) sits at constant row-distance 2 inside the REAL explosionRadius 3; a third snake (row 36) is far outside it. Asserts exact direct-hit and splash percentages, zero loss outside the radius, the floor clamp, and that heldPowerup is never set (auto-fire proven). Additional scenarios: single-player fizzle (+1, no shell, no explosion), dead-but-connected self-nuke, and testHook prod-inertness. The old test's explosionRadius:100 geometry dodge and best-effort splash observation are gone; splash geometry is now hard-asserted. pw_blueshell is back in the counted suite.
- tests/pw_wormhole.js: added self-collision rescue scenarios (plain and while boosting, staged with the new hooks) after the maintainer reported the charge not saving from self-collision -- investigation showed every death path (wall/self/snake/head-on) already routes through the tryWormholeOrDie interception and the rescue works; the report was most likely a landing-scan fizzle (which still consumes the charge) or an already-spent charge. The behavior is now locked in by permanent coverage.

#### Split-step head-on fix (round nineteen) -- v3.0.3
- Fixed the head-on collision case players actually hit: v3.0.2's fix covered two snakes moving in the SAME movementStep, but movement runs on per-snake accumulators (s.moveAccumMs), and two snakes that joined at different times cross the movement threshold on DIFFERENT sim ticks -- so in a live game two equal-speed snakes almost never move in the same movers set, and the same-step head-on rule never fired. A real-play head-on resolved sequentially instead: the snake whose tick came first landed on the other's stationary head, counted as a plain body ram, and died ALONE -- the stationary snake survived every head-on. Fix (resolveSnakeCollisions): a mover whose new head lands exactly on a non-mover's HEAD cell while their directions oppose is a head-on -- both die, no kill credit. A perpendicular landing on a head cell stays a normal T-bone ram (only the mover dies). tryWormholeOrDie now falls back to the stationary snake's own head as the fatal cell (a non-mover has no newHeads entry), so a charged victim's wormhole still fires instead of crashing.
- tests/pw_headon.js now covers BOTH tick alignments. Since alignment cannot be forced from outside the server (join timing vs. the 16ms sim grid is jittery), it runs collision rounds -- varying spawn gap parity and staggering the second join by half a move interval -- until both a same-tick and a split-tick head-on have each produced a passing mutual death, asserting the invariant every round. Verified to fail on the pre-fix server exactly as reported (one snake dead, one alive). The v3.0.2 version of this test passed while real play failed because both test seats join inside the same 16ms sim tick on localhost -- the one alignment real players never get.

#### Powerups fire on pickup, activate-key remap, head-on fix, powerup feedback visuals (round eighteen) -- v3.0.2
- Powerups now AUTO-ACTIVATE the instant they are collected -- the timed self-buffs (Ice Trail, Poison Trail, Growth Spurt) fire on pickup with no button. This resolves the maintainer's "ice/poison trails don't render" report: a live headless-Chromium repro confirmed the trails always drew and the mechanic always worked; the real problem was that trails required a button press to lay, and in normal play that press never happened (compounded by the activate-key confusion below), so no trail was ever created. Speed Boost still holds and fires on the activate key (you want to time your own speed). Blue Shell also stays button-fired FOR NOW -- auto-firing it on pickup is deferred to Phase 9, which lands it together with the single-player guard, because until that guard exists a lone player would auto-fire the shell at the longest snake (themselves) the instant they collected it; it is disabled by default regardless. Wormhole is unchanged (its own auto-triggering charge, never in the held slot). Implemented data-driven: a module opts into the held-slot / button-fired behavior with `requiresActivation: true` (speedBoost and blueShell set it), server.js builds a HELD_TYPES set from that, and a shared firePowerup() helper runs the activation effect from both the pickup path (auto types) and the activate message (held types). e2e updated to assert the new pickup semantics (pw_icetrail/poisontrail/growthspurt fire on pickup; pw_speedboost unchanged).
- Swapped the default activate keys: player 1 (arrow keys) now activates with Right Shift -- right next to the arrows -- and player 2 (WASD) activates with Space. This was the actual cause of "Speed Boost won't activate": the old mapping put Space on p1 and Right Shift on p2, so a solo player on the arrows pressed Right Shift (which belonged to p2) and nothing happened. The mechanic itself always worked (a real key-press repro shows moveMs drop from 200 to 125 on activation). The in-game KEYS rebind panel already renders the bound key dynamically, so it reflects the swap with no other change; a player who had customized their activate key keeps their override.
- Fixed head-on collisions not killing both snakes. In a head-on SWAP -- two snakes trading cells in one step -- resolveSnakeCollisions resolved the first snake's death, then SKIPPED the second because the first was already flagged dead (`|| died.has(j)` in the inner scan), so the second snake glided through the still-solid corpse and survived. A snake that dies on a given step is not removed from the board until handleDeath runs at the end of that step, so it must still count as a solid obstacle during collision resolution. Fix: stop skipping died snakes in the scan; compute a per-opponent "actually moves this step" flag (not stalled AND not died) for both the same-cell head-on test and the tail-skip rule; and add clearMutualKills() so a mutual swap credits no kill (matching the same-cell head-on, which already credited none). New regression test tests/pw_headon.js covers both the same-cell meeting (even head gap) and the swap (odd gap), staged deterministically via the new spawn hook below.
- Added SNAKE_TEST_SPAWNS, a test-only environment hook (inert unless set): a JSON array indexed by slot, each `{x, y, dir}`, forces deterministic snake spawns with the body auto-oriented behind the head. Lets a test place two snakes on one row facing each other and collide them in one or two ticks, instead of walking randomly-spawned snakes together (the fragile staging that has long blocked the Blue Shell splash-geometry test -- this hook is the tool to fix that next).
- Powerup activation and duration are now VISIBLE on the snake itself, three related cues, all server-authoritative-data-driven and implemented identically in the wasm renderer and the 2D fallback (covered by the frozen-clock pixel-diff, still <=0.001% differing pixels):
  - Timer tail-drain (the countdown): while a timed powerup is active the head-side N = ceil(remaining-fraction x length) body segments are tinted the powerup's color. At activation the whole snake is colored; as the effect runs down the tint drains tail-first back to the player's color, so the amount of colored snake IS the time remaining. The server broadcasts the remaining fraction per player (activePct); the renderers colour segments from it.
  - Activation flash: a brief bright pop of the powerup color over every segment on the tick a powerup fires -- the unmistakable "it worked" cue, and for a one-shot like Blue Shell the only such cue. Driven by a one-shot `activated` marker per player in the broadcast (cleared the next tick, like the teleport/explosion one-shots), aged out client-side over ~380ms.
  - Speed Boost active jetstream: while Speed Boost is running the snake trails a persistent jetstream in the powerup's orange, the same shape as the hold-boost jetstream but a distinct color, so "you are currently fast from the powerup" reads at a glance and stacks visibly with hold-boost.
  All three are gated by new clientFx.powerupFx (default true), served through /api/config like the other cosmetic toggles. Two new fields in the wasm player struct (activeIdx, activePct, stride 48->56) and a new per-frame activation-flash array carry the data; the parity scene (tools/bench/scene.js) was extended to exercise active powerups, the tail-drain, the jetstream, and the flash so the pixel-diff actually covers them.

#### Debug panel powerup + drift feedback (hotfix) -- v3.0.1
- Added powerup state visibility to the DEBUG panel: each player now shows held/active powerup types so the operator can see what is queued for activation and what effect is currently running. This fixes the gap where a failed activation attempt (no held powerup) was silent and indistinguishable from the server not receiving the message.
- Added drift and activation-button press indicators to the DEBUG panel status line: "DRIFTING" flags when an active powerup is one of the trail types (iceTrail or poisonTrail, which apply server-side movement slowdown), and "PRESSING-ACTIVATE" shows when the bound activation key is currently held, so a player can debug why their powerup won't fire (usually: they don't actually have one).
- Fixed a keydown handler edge case: when the activation key is pressed while a direction key is held, refreshBoost() now runs immediately afterward (was missing the early return before the function ended), ensuring the boost state is always current even in rapid multi-key sequences.
- Optimized tools/build-wasm.mjs: added cache check comparing wasm/renderer.ts source sha256 to the one embedded in render-wasm.js (already present in the generated file header). Skips compilation if unchanged, saving ~5-10 seconds per CI/deploy build.

#### WASM renderer, boost ramp/grace, held-powerup glow, server perf pass (round sixteen, Phase 7) -- v3.0.0
- Rewrote the client's per-frame draw logic in WebAssembly (wasm/renderer.ts, AssemblyScript, compiled -O3 with the no-GC stub runtime -- the module allocates nothing after init). JS encodes each server snapshot into the module's linear memory once per broadcast (a curr/prev pointer flip means the previous snapshot is never re-encoded), writes a small per-frame effects block, and the module emits an ordered instance buffer of colored shapes (rects / ellipses / rings) that a thin executor plays onto the canvas. Full feature parity with the old renderer -- trails, food + predicted-eat hiding, pickup pulse, blue shell + explosion ring, lock-step interpolation with the drift-diagonal glide rule, correction glide, predicted local bodies, dead overlay, jetstream, slide dust, input flash -- verified by pixel-diffing both renderers on identical frozen-clock scenes (tools/bench/parity.html: <=0.001% of pixels differ, all on anti-aliased ellipse edges).
- The executor is the 2D canvas context, NOT WebGL -- benchmarked first per the maintainer's "WebGL, or whatever is more performant." On real hardware (RTX 3080, ANGLE/Vulkan, headless Chromium with vsync disabled only for measurement) GPU-accelerated canvas 2D beat a WebGL2 instanced-quad pipeline at every board size: 4k-preset stress scene (4x1000-segment snakes + 800 trails + all effects) 99fps canvas-2D vs 24fps WebGL2. The shipped wasm+2D path matches or slightly beats the old renderer (104 vs 99fps there, p95 frame 13.3 vs 14.0ms) while moving all geometry/interpolation math out of JS. The instance-buffer architecture is executor-agnostic, so WebGL can be revisited without touching the wasm. The game itself remains requestAnimationFrame-paced, i.e. vsync -- nothing about frame pacing changed.
- The compiled artifact (public/js/render-wasm.js, ~9KB of wasm embedded as base64 in a plain JS file -- no separate fetch, no MIME changes; the ~33% base64 overhead is noise at this size) is deliberately NOT committed: install.sh builds it from source on the target host (new step between clone and deploy, non-fatal on failure) and the release workflow builds it and packages it into the archives, so what runs is verifiably compiled from the tree. tools/build-wasm.mjs runs under both node (npx fetches the pinned assemblyscript@0.28.19) and deno. The old renderer lives on intact as public/js/render2d.js; render.js is now a facade keeping the exact Render.draw/POWERUP_STYLE surface, selecting via new config clientRender.renderer ("auto" default / "wasm" / "2d" as the operator kill-switch) or a ?renderer= URL override, and falling back to 2D automatically when the artifact is missing (a bare source checkout still just works) or instantiation fails. Verified live in headless Chromium over CDP: real captcha solve + join on both paths, correct build stamps, lit canvas, screenshots.
- Held-powerup glow: a snake carrying a powerup -- or an armed wormhole charge -- now glows in that powerup's color, a pulsing halo under every segment, visible to EVERYONE so opponents can see what is coming and plan counterplay (the heldPowerup/wormholeCharge fields were already in the shared broadcast; this just surfaces them). Implemented identically in the wasm renderer and the 2D fallback (covered by the parity diff), gated by new clientFx.heldGlow (default true).
- Boost feel redesign (server-authoritative): boost no longer snaps to full speed. A hold must survive new boost.holdGraceMs (default 120) before boost engages at all -- a short tap no longer speeds the snake up NOR tags a queued turn with a drift -- then the multiplier ramps linearly from 1x to boostSpeed over new boost.rampMs (default 400). The drift window now scales with the ramp: a turn stores the ramp progress at keypress time and skids for driftMs * that fraction, so a barely-boosting snake barely skids while a full-speed one gets the full window. Wall-grace forfeiture and the broadcast boost flag (jetstream) now key off "engaged", not the raw key. The client mirrors the grace so its predictor keeps pre-playing turns made inside it (they are plain turns), and the join-screen tip + debug panel explain the ramp. rampMs: 0 / holdGraceMs: 0 restore the old instant behavior exactly -- which is how the pre-existing boost tests now pin their timing-sensitive assertions (pw_boostdrift.js, pw_speedboost.js -- the latter actually caught the interaction: its stacking assertion sampled moveMs mid-ramp and failed until pinned, exactly what the suite is for). New tests/pw_boostramp.js covers the grace (sub-grace tap changes nothing but direction), the ramp (moveMs strictly between unboosted and full mid-ramp, exact full value after), and the drift scaling (early-ramp turn skids ~120ms vs ~950ms at full ramp, measured on the live broadcast).
- Server performance pass, profile FIRST as the maintainer specified: new SNAKE_PERF=1 env-gated instrumentation (inert nulls when unset, same philosophy as dlog) times movementStep/broadcastState at their call sites, counts broadcast bytes, and prints a [perf] JSON line every 5s; new tests/perf_baseline.js drives 4 live circling boosted clients through {96x54, 192x108} x {3, 60, 120 spawn length} scenarios and tabulates it. Verdict: there is no server CPU problem to fix -- movementStep averages 25-60us and broadcastState 115-145us at ~20 broadcasts/s (~0.3% of one core, worst case measured), broadcast size ~19 bytes/segment (~9.5KB with 480 segments on board), RSS ~90-105MB dominated by the runtime. Board size barely matters; segment count is the only real scale factor. Accordingly only one allocation fix was kept (expirePowerupsAndTrails no longer reallocates the trails array on every movement step when nothing expired) and the heavier candidates were REJECTED as unjustified by measurement: preallocated movementStep maps, a hand-rolled body serializer, and the flat-array wire format (maintainer had approved that one only if serialization dominated -- it does not). The instrumentation and harness stay for the next time this question comes up.
- Test/verification infrastructure added under tools/bench/ (not shipped to players): bench.html/bench.js (frame-time benchmark of 2D vs wasm vs WebGL2 on deterministic synthetic scenes), parity.html/parity.js (the frozen-clock pixel diff), run-bench.js (serves the repo, drives headless flatpak Chromium over CDP, extracts results), and run-smoke.js (boots the real server, solves the real captcha through the DOM, joins, asserts per-renderer build stamps + lit canvas, screenshots). Full 8-test pw_*.js suite green after all server changes.

#### Boost slide redesigned into a real drift (round fifteen) -- v2.1.1
- Redesigned the boost turn from a delayed input into a real drift, per the maintainer's spec. Old mechanic: a turn queued while boosting was tagged delay = slideDistance and the whole snake kept going straight for that many cells before the turn registered. New mechanic: the turn is applied to the HEAD immediately (verified e2e: dir flips on the very next movement step after the input), and instead the whole body keeps skidding one cell per movement step in the PREVIOUS travel direction for a time window -- so the head carves diagonally out of the corner while the body visibly slides sideways, a skid rather than an unresponsive input.
- Maintainer's three collision/tuning decisions, all implemented: (1) the drift window is TIME-based -- new config boost.driftMs (default 250) replaces boost.slideDistance, which is now ignored if an old config still carries it; (2) the skidding body is physical but CLAMPS at obstacles -- the per-step translation is skipped entirely (rigid body, no deformation) if it would push any segment out of bounds or into another snake, so the skid grinds against walls instead of killing its owner, while the drifted body stays fully solid to everyone else automatically since collision scans read live positions; (3) NO wall grace while boosting -- a boosted head aimed at a wall with no saving queued turn dies without the stall tick (verified e2e: death exactly 1 seq after reaching the last column boosted, exactly 2 seqs unboosted).
- Implementation: the body translation runs as a new applyDriftSlides pass at the top of movementStep (before new heads are computed, so the head extends from the translated front and the snake stays connected); the drift starts when a drift-flagged input (tagged at enqueue while boosting, same as before, so releasing boost right after the keypress doesn't cancel committed momentum) is applied in computeNewHeads, recording the pre-turn direction; the old inp.delay machinery is gone. The broadcast `sliding` flag now reflects the drift window, driving the existing slide-dust client effect unchanged. A wall-saving turn consumed early by consumeInboundsTurn deliberately does not start a drift.
- Client: predict.js still does not pre-play a turn typed while boosting -- previously because the landing tick was unknowable, now because the same server step also translates the body, which a one-cell follow-the-leader predictor cannot reproduce; behavior is identical, the reasoning comment changed. render.js segPixel's interpolation window widened from exactly-1 to 1-2 Manhattan so a drifting snake's diagonal segment steps still read as a glide instead of snapping. Debug panel shows driftMs where it showed slideDistance; the join-screen boost tip and README describe the new mechanic (including the no-mercy-at-walls rule).
- New e2e test tests/pw_boostdrift.js (Deno WS client against a live server, the standard harness): immediate head turn, sliding=true during the window with the head provably carried sideways while descending, drift expiry ending the sideways carry, boosted wall death with no grace, and the unboosted grace tick unchanged as a contrast case. Full existing six-test powerup suite re-run green after the movementStep change.
- Phase 10 note (drift EFFECTS): its premise -- underselling "the turn not happening for slideDistance cells" -- is obsolete; the drift is now itself the visible feedback. The ghost-path idea (showing where a delayed turn will land) no longer applies; dust/lean/smear ideas still do.

#### Post-v2.1.0 visual/UI fixes: trail visibility, top-bar fade, segment gaps (round fourteen) -- v2.1.1
- Made ice/poison trail tiles actually readable. The mechanics were confirmed working (the e2e suite asserts the slow and the damage) and the tiles were confirmed reaching drawTrails; the problem was purely styling: 0.35-0.4 alpha tints sit directly on the black background (not over other content), which rendered as near-black -- rgb(49,77,89) for ice -- and disappeared entirely after a fractional downscale. Raised the tints to 0.6-0.65 alpha with slightly brighter bases (ice now reads at rgb(98,146,166), poison at rgb(66,126,42) on the live canvas).
- Fixed the top bar never fading back out. Hiding used to depend on receiving a pointermove BELOW the 80px reveal zone, so parking the pointer on the bar, or leaving the window out the top edge, meant no qualifying move ever fired and the bar stayed at full opacity forever. Hiding is now time-based from the last activity: every in-zone pointer event re-shows the bar AND re-arms a single idle timer, moves below the zone need no handler at all, and a document pointerleave arms the timer for the out-the-window case. Panel pinning (DEBUG/KEYS open) still holds the bar visible and re-arms the timer on unpin; the pin counter itself was audited and has no stuck path (both toggles are symmetric and neither button is ever re-created).
- Fixed snake segment gaps vanishing at non-fullscreen sizes, two complementary halves: (1) the inter-cell gap now scales with cell size (max(1, round(cellSize * 0.08)) internal pixels, so 2px at the default cellSize 20) instead of a fixed 1px that lands between destination pixels and disappears under any fractional downscale; (2) fitCanvas() applies image-rendering: pixelated only on integer upscales, switching to smooth filtering on downscales, where nearest-neighbor is exactly what was eating the gap. The gap constant also drives the trail tiles, input flash strip, glide head, and dead-snake overlay fills so every cell-shaped draw stays consistent.
- Verified all three in headless Chromium over the DevTools protocol against the live server: trail/food/gap pixels read back from the canvas at a fractional downscale (trail tints at their full intended values, 2px background gap present, image-rendering "auto"), and the top bar observed fading after idle in both previously-stuck cases (pointer parked in the reveal zone; pointer left the window) as well as re-revealing on the next in-zone move.
- Deliberately NOT built yet: the maintainer's fourth report (redesign the boost slide so the head turns immediately while the body drifts) is a server mechanic change whose collision semantics the handoff explicitly requires the maintainer to spec first (drift length, whether the sweeping body collides mid-drift, and whether wall-grace still consumes the turn early).

#### Screen-size presets, display scaling, and mobile controls (round thirteen, Phases 5+6) -- v2.1.0
- Replaced the fixed 50x30 board with named 16:9 presets: config grid.preset "1080p" (96x54 at cellSize 20, the new default -- exactly fills a 1920x1080 display) or "4k" (192x108 -- more cells at the same cell size, intended for high-end servers). Explicit grid.cols/rows still work and always win over a preset, so hand-tuned operator configs and the test suite's per-test board overrides behave exactly as before. The server resolves the preset once at startup; the wire format is unchanged since the resolved grid was already broadcast in every state message.
- Separated the canvas's internal resolution (still exactly cols*cellSize x rows*cellSize) from its on-screen size: a new fitCanvas() sizes the canvas via CSS to the largest aspect-preserving fit of the viewport on every resize, so the board letterboxes/pillarboxes instead of distorting or overflowing when the window is not 16:9. Upscales snap down to an integer multiple with image-rendering: pixelated so enlarged cells stay crisp; windows smaller than the board get the exact fractional fit instead (there is no integer scale below one).
- Moved all four corner buttons (DEBUG, add-player-2, KEYS, Leave) into a single fading top bar that reveals when the pointer comes within 80px of the top edge (or on a tap up there) and fades out after 1.5 seconds of the pointer being elsewhere. The bar stays pinned visible while the DEBUG or KEYS panel it spawned is open, so a toggle button can never fade out from under its own open panel. Button behavior is unchanged -- this is a relocation, not a redesign.
- The leaderboard panel no longer sits beside the board (which only worked when the board was narrow): it is now a fixed overlay in the top-right, faded to near-invisible during play and fully opaque on hover, since it is not important mid-game.
- Added mobile/touch controls, gated by coarse-pointer detection so desktop is untouched: swipe on the board to turn (dominant axis of a >=24px drag, one turn per swipe, feeding the exact same input path as a keypress so prediction and netcode needed no changes -- and a swipe when you have no seat is the rejoin gesture, mirroring the keyboard rule that the input itself is the join request), plus a tap PWR button for powerup activation and a hold-to-boost BOOST button whose state folds into the existing held-key boost detector so the server still sees only on/off transitions. Mobile is deliberately single-seat: couch co-op assumes one keyboard, not one phone, so the add-player-2 button is never built on touch devices. A touchcancel is treated as boost release so an interrupted touch can never leave a snake boosting forever.
- Added a powerup color legend to the captcha/join screen: one swatch + name per powerup so a new player knows what each pickup color means before their snake ever moves. Colors are read from the renderer's own style table (the exact colors the board draws with, one source of truth), and types the operator disabled are skipped, so the legend can neither drift from the board nor advertise a powerup that cannot spawn.
- Verified end to end: both presets confirmed on the wire with scripted WebSocket clients (96x54 default, 192x108 for 4k), the full six-test powerup suite passing against the new default config, and the real join flow driven in headless Chromium over the DevTools protocol -- desktop (1920x930 window: correct fractional downscale, top bar hidden at rest and revealed on hover) and emulated mobile (390x844 touch device: letterboxed board, PWR/BOOST present, no co-op button, and a synthetic swipe actually turning the snake on the server).

#### Deploy fix: ship the powerups/ modules (round twelve hotfix) -- v2.0.1
- Fixed the installer and the manual-install docs to deploy the new server-side `powerups/` directory. v2.0.0 added `require("./powerups")` to `server.js` but neither `install.sh` nor the README's manual steps copied that tree to the app directory, so a fresh install crashed on boot with `Cannot find module './powerups'` before the service ever listened (the exact "a new server-side directory fell out of the file list" failure the `public/` tree-walk was already written to prevent). `install.sh` now walks and syncs `powerups/` the same way it does `public/` (including stale-file cleanup), and the manual-install section copies it with a note explaining why it is required. No gameplay change.

#### Powerups, and a shell with the leader's name on it (round twelve, Phase 4) -- v2.0.0
- Added a powerups system: pickups spawn on the board on their own cadence (config powerups.spawnIntervalMs, capped at powerups.maxConcurrentPickups, default 1 at a time), each a distinct type with its own config block, all gated by an individual enabled flag. Every type's short player-facing description lives in its own module (powerups/*.js) and is served through /api/config, so the in-game help can never drift out of sync with what actually exists. The six types:
  - Wormhole: held in reserve as an independent charge -- no button. The instant a move would kill you (wall or another snake), it auto-fires, phasing you to the nearest safe spot or clear across the board, whichever is safer. One-time use; firing (even a failed landing) consumes it.
  - Growth Spurt: for a duration, food grows you twice as much and kills grant bonus length.
  - Speed Boost: a timed speed multiplier that stacks multiplicatively with the existing hold-to-boost.
  - Ice Trail: while active you lay a trail; anyone who crosses it (you included) slows more each time, floored so it never fully stops.
  - Poison Trail: while active you lay a trail; anyone who crosses it (you included) loses a segment, down to the global minimum length.
  - Blue Shell: activating it launches a seeking projectile at whoever is CURRENTLY longest -- even you, if you are the leader -- passing harmlessly through every other snake in flight. On impact the leader loses a large percentage of their length and it explodes, splashing a smaller percentage off nearby snakes. This makes leading risky and picking up every powerup no longer automatic. Shipped DISABLED by default (config powerups.blueShell.enabled: false) pending a more robust splash-damage test -- see README; the mechanic itself is complete and can be turned on by an operator.
- Inventory model: one held slot per player for the next non-wormhole powerup plus an independent wormhole charge. A pickup that would be wasted (slot already full of that type, or wormhole already charged) instead grants a food-like +1 segment. A per-seat activation key fires the held powerup (default Space for the arrows seat, Right Shift for the WASD seat); wormhole ignores the button entirely.
- Added a global minSnakeLength (default 3) that is now the single source of truth for BOTH the initial spawn length and the poison-trail damage floor, so the two can never drift apart.
- Trails and pickups are separate entity lists broadcast alongside food; trail tiles expire on the sim clock (not wall-clock timers), and a laid tile never permanently modifies a board cell. Food placement now also avoids spawning on top of an existing pickup.
- Client: pickups render as pulsing tiles, trails as dim tinted cells under the snakes, the blue shell as a moving projectile with an expanding explosion ring on impact, and a wormhole teleport snaps instantly (predict.js checks a one-shot teleport flag before its normal correction-glide path so a jump never animates as a slide across the board).
- Keybind remap UI (bottom-left corner panel, matching the existing debug/coop/leave button pattern): rebind each seat's activation key and swap which local index uses WASD vs. arrows. Persisted in localStorage, entirely client-side -- the server only ever learns which local index activated, never the physical key.
- Captcha/join screen: replaced the wormhole-only explainer with a single "What do the powerups do?" button whose popup is built from the server's powerupInfo and auto-sizes to however many powerups are enabled. Descriptions are short and ESL-friendly.
- Boost & slide follow-ups (same mechanic, shipped alongside): bumped the defaults to a more noticeable boostSpeed 2.0 / slideDistance 3; the debug panel now shows the live boost and slide amounts; added purely-cosmetic visual indicators (a jetstream behind a boosting head, a dust scatter during a slide drift), each gated by a clientFx flag; and fixed the "Got it" boost-tip button, which previously only dimmed the tip -- it now hides it outright and is fully independent of captcha state.
- Committed an end-to-end test suite (tests/pw_*.js): scripted Deno WebSocket clients that drive the live server and assert on real state broadcasts, one per powerup, using a shared retry harness for the inherently nondeterministic snake-steering setup. This round's test work found and fixed real bugs: the wormhole originally excluded a reversing escape direction (so it fizzled at walls), iceStacks was computed but never broadcast, and -- caught by the suite itself -- every pre-existing test needed to explicitly disable the newly-added blue shell so it wasn't collected by accident.

#### Initial game and deployment (round one)
- Created the single-player browser Snake game as a self-contained HTML file with a canvas renderer, arrow key and WASD controls, wall and self collision, a score display, and restart after game over.
- Replaced the fixed-rate game loop with a self-rescheduling timer so the tick delay can change during play. The game starts slower and speeds up as food is eaten, down to a configurable floor.
- Replaced the single stored direction with a capped input buffer. Each turn is validated against the last queued direction rather than the committed one, which closes a reversal that could kill the snake when two keys were pressed inside one tick. The buffer holds at most two turns and drops no-op repeats.
- Rebuilt the game as an authoritative multiplayer server in Node.js. One process holds the game state, serves the client, and exposes the captcha endpoints. The board supports one to four live players with no accounts.
- Added a spectator queue for connections past the fourth player. A spectator takes over a freed slot after a short delay so they have time to take control. When four or fewer players are connected, a dead player simply respawns.
- Added player-versus-player scoring. Running into another snake body kills the mover and awards the other player a 10 point bonus with 3 segments of growth. Head-on collisions kill both players with no bonus.
- Added daily and all-time top 5 high score boards with arcade-style 3 letter initials entry, persisted to a JSON file. The daily board resets when the calendar date changes.
- Added a simple math captcha that issues a one-time, short-lived token before a WebSocket connection is accepted. It is intended to sit behind a Cloudflare filter, which is expected to do the real bot filtering.
- Fixed a spawn placement check that could drop a new snake on top of an existing one, so a fresh snake now spawns only where all three of its starting cells are clear.
- Bound the Node process to 127.0.0.1 so it is not directly reachable, with Apache as the sole public entry point. Added a 30 second WebSocket keepalive ping because Cloudflare closes idle proxied WebSocket connections after about 100 seconds.
- Added an Apache reverse-proxy virtual host on a dedicated hostname so the game does not collide with the existing static site served from another virtual host on the same server.
- Added a systemd unit, a one-command install script, a one-command uninstall script, and setup documentation. The installer detects an existing checkout or clones the repository, installs Node.js when needed, deploys the app, creates an unprivileged service user, and adds the Apache virtual host without modifying existing sites. The uninstaller backs up the high score file before removing the app.

#### Generic hostname, TLS, and packaging (round two)
- Made the deployment hostname generic. The repository now ships a single Apache vhost template, deploy/fillmeout.example.com.conf, instead of a vhost named for one specific domain. The installer copies the template and substitutes the chosen hostname into /etc/apache2/sites-available/<hostname>.conf. The earlier fixed-name vhost was removed.
- Added an interactive hostname prompt to the installer. On the first run there is no default and a valid hostname must be entered. On a later run the previously used hostname, saved in /etc/multisnake/last-domain, is offered as the default and reused when the prompt is answered with an empty line. An explicit DOMAIN environment variable still overrides the prompt for non-interactive runs, and the prompt reads from the controlling terminal so it also works under the curl pipe.
- Added retirement of a previous hostname on the installer. When a repeat run uses a different hostname than the saved one, the installer disables and removes the old vhost files, including the certbot-generated -le-ssl variant, so an orphaned site is not left enabled alongside the new one.
- Added TLS provisioning to the installer using Let's Encrypt through certbot. The initial implementation used the HTTP-01 (port 80) challenge with the Apache plugin, then it was changed to the DNS-01 challenge through the Cloudflare API. DNS-01 was chosen because it needs no inbound port 80, so the DNS record can stay proxied through Cloudflare, whereas HTTP-01 fails when Cloudflare proxying or the Always Use HTTPS setting intercepts the challenge request. The Apache installer still writes the :443 vhost and the 80 to 443 redirect. The game is served on 443.
- Made TLS setup safe on hosts that already run certbot. Certbot and its plugins are installed only when certbot is not already present, so an existing certbot and its renewal schedule are left alone. The installer never edits certbot.timer or any cron job, since installing the Debian package already provides a twice-daily renewal timer that covers every certificate, including this one. If a certificate for the hostname already exists it is reused rather than reissued, which avoids rate limits and does not disturb other certificates.
- Added handling of the Cloudflare API token and email for TLS. The token can be provided as CF_API_TOKEN, reused from an existing per-hostname credentials file, or entered at a hidden prompt. It is written to /etc/letsencrypt/cloudflare-<hostname>.ini with mode 600 and reused at each renewal, since certbot records the path but not the contents. The email can be provided as CERTBOT_EMAIL or entered at a prompt, and a blank value registers without an email. TLS can be skipped entirely with ENABLE_TLS=no, and the propagation wait is adjustable with CF_PROPAGATION.
- Updated the uninstaller for the generic flow. It reads the hostname from /etc/multisnake/last-domain, or from DOMAIN if set, so it removes the correct vhost, certificate, and Cloudflare credentials file no matter which domain was used. It removes both the plain and the -le-ssl vhost files, deletes only this hostname certificate with certbot delete so other certificates and the shared renewal timer are untouched, removes the per-hostname credentials file, backs up the high score file to /root, and removes the installer state directory. REMOVE_CERT=no keeps the certificate and credentials.
- Added a source-snapshot release workflow. Because the project ships source only with nothing to compile, the workflow packages the tagged commit into .tar.gz and .zip archives with git archive and attaches both to a GitHub Release, rather than building binaries. Base token permissions are read-only with write elevated only on the release job, a concurrency group keyed on the tag prevents a re-pushed tag from racing an in-progress publish, and the publish step fails if no archive is produced. Action pinning to commit SHAs is left off by default with a directive to ask before enabling it, since pinned actions require ongoing maintenance the project does not have.
- Added community-health files filled in for this repository: a Code of Conduct based on Contributor Covenant 2.1, a Contributing guide with local test steps for the game, a Security policy pointing at GitHub Security Advisories, bug report and feature request issue templates with valid front matter, and a pull request template. Placeholder tokens for the project name and repository URL were replaced with the real values, and the security policy support table was converted to plain text to keep the files ASCII.
- Wrote the project README covering the feature set, requirements, the one-command install and uninstall, the interactive hostname prompt, the DNS-01 TLS flow with Cloudflare token scoping, the configuration keys, service operation, the repository layout, and the known limitations.

#### Port selection and failure detection (round three)
- Made the listening port dynamic instead of fixed at 8080. The installer selects a port before anything binds: an explicit PORT override if set, otherwise the port saved from the previous run, otherwise 8080, scanning upward until a free port is found. The chosen port is written into server.js and substituted into the Apache vhost alongside the hostname, and saved to /etc/multisnake/last-port for reuse. This was prompted by a real deployment where port 8080 was already held by another service on the host, which the earlier fixed-port installer could not accommodate. The vhost template and server.js still ship 8080 as their default placeholder, since that is the value the installer substitutes.
- Added a post-start health check to the installer. Because the systemd unit is Type=simple, systemd reports the service as started the moment it forks, so a crash immediately after (for example a port already in use) previously went unnoticed and the installer continued on to issue a certificate for a process that was not actually running. The result was a confusing 404 served by whatever else held the port. The installer now requests the app on its port after starting the service and aborts with the recent service logs if it does not answer.
- Added detection and recovery for a previously failed install. On a repeat run the installer checks whether the existing service is in a failed state or whether its logs show an address-in-use error. If so, it reports which port the failed install was using and which process currently holds it, explains that this is the likely cause of a 404 after the certificate step, and, when run interactively, asks for confirmation before selecting a free port and re-pointing the app and both Apache vhosts, including the certbot SSL vhost that certbot will not rewrite when the certificate is reused. This recovery path is separate from the proactive port selection, whose role is to prevent the clash on a clean run rather than repair an existing broken one.

#### Client netcode split and prediction (round four, Phase 2)
- Split the monolithic inline client into five ES modules under public/js: net.js (WebSocket transport and snapshot storage), predict.js (client prediction and reconciliation), render.js (canvas drawing), ui.js (captcha gate, status, high score lists, overlays, debug panel), and main.js (bootstrap and input). index.html loads them in dependency order. The installer already deploys the whole public tree, so the split deployed without a path change.
- Fixed a first-turn-only input bug. The client validated each new turn against a stale local heading, so after the first accepted turn later keypresses were rejected before being sent. The server now includes each player authoritative dir in the snapshot and the predictor adopts it, with a body-vector fallback for older snapshots.
- Removed all sub-cell interpolation. Rendering is grid-snapped to whole cells so motion is the classic discrete Snake step (tail cell drops, a cell appears at the head) rather than a smooth glide.
- Reworked the authority model to client-lead movement with server reconciliation. The client simulates its own snake from local input and renders that directly. The server stays authoritative for collisions, food, and score; it overrides the client position only on a real conflict (death or a respawn/teleport jump), never on ordinary one-tick timing differences.
- Added client-favoring wall grace on the server. A turn that avoids a wall is honored even if it arrives a tick late; the snake holds in place for a configurable number of grace ticks (wallGraceTicks) rather than dying to a late-by-a-hair input. Walls are static so this cannot be abused.
- Added input acknowledgment for true server reconciliation. Each client turn carries an incrementing sequence id (cseq); the server records the last sequence it processed per player and stamps it into each snapshot (you.ack). The client drops acknowledged inputs and replays only unacknowledged ones on top of the authoritative body. The server also acknowledges rejected turns (reversal, duplicate, buffer full) so the client stops retrying them, and resends unacknowledged turns after a couple of ticks to recover from dropped packets.
- Added client-side food prediction with rollback. The client predicts its own eat for instant feedback and hides that food, then confirms it when the server food changes and the body grows, or rolls the growth back if the server does not confirm within a few ticks. This addressed food that was not registering and food that appeared eaten while circling it.
- Fixed the repeated high-score prompt. Score is reset to zero on every respawn, and a zero or negative score never qualifies, so a respawned snake can no longer re-qualify with a stale score.
- Reworked the death and join lifecycle. On a qualifying death the player is gated on initials entry and cannot respawn until they submit; if they do not within initialsTimeoutMs (default 20 seconds) they become a spectator. Spectators sit in a global queue and are disconnected after spectatorIdleMs (default 5 minutes) of idle time. When a slot opens the front spectator is offered it with an explicit join button (joinOfferMs, default 10 seconds); if they do not accept, the offer passes to the next spectator, so an idle spectator cannot silently take a slot.
- Added an on-page DEBUG button and panel (no dev console needed). It shows client and server build stamps, the current sequence and tick, the pending input buffer, and recent server corrections. Recording is off until the panel is opened so there is no cost on the hot path. Every module registers a build stamp in a shared registry so a partial or stale deploy is immediately visible.
- Added Cache-Control headers on the Node static handler (no-cache for js, css, and html; no-store for the captcha API). A stale Cloudflare edge cache had been serving old JS after deploys; origin cache headers stop the edge from holding stale code.

#### Decoupled simulation rate (round five)
- Decoupled the server simulation rate from the snake movement cadence. A fixed loop runs at config simHz (default 60) and samples input every tick, while the snake advances one cell only when a movement accumulator crosses the current move interval. This lowers the input sampling delay to about one sim frame without changing snake speed, which is still governed by the movement cadence.
- Expressed the speed ramp as milliseconds per cell (config move.startIntervalMs, move.minIntervalMs, move.rampIntervalSec, move.rampStepMs) instead of the old tick-based speed block. Setting move.rampStepMs to zero gives a constant speed. Full state is broadcast only on movement ticks, so bandwidth is unchanged, and the network sequence still counts movement steps so client reconciliation is unaffected.
- Added a simHz prompt to the installer, defaulting to 60 and offering the last used value on repeat runs, saved to /etc/multisnake/last-simhz. It can be forced non-interactively with SIM_HZ, is validated as a positive integer in the range 1 to 1000, and is written into the deployed config.json with Node so the file stays valid JSON.

#### Prediction bounded to one cell and matched to server order, plus feel (round six)
- Fixed a rendering jitter regression in the client predictor. rebuild() had been advancing the locally rendered body by one cell every tick using the last known direction, even when there were no unacknowledged inputs queued, which left the rendered head a constant one cell ahead of the authoritative head and produced a visible wobble on ordinary straight-line movement and on turns. The predictor now renders the authoritative body exactly whenever its local input queue is empty, so no input means no prediction and the server can never appear to lag behind or override a plain straight run.
- Bounded client prediction to exactly one cell ahead of the authoritative body at all times. An intermediate fix had instead replayed one cell per queued input, which produced a visible multi-cell jump whenever a second turn was queued before the first was acknowledged. Also stopped the debug panel from logging a false correction on ordinary straight-line movement with an empty input queue: comparing a deliberately un-advanced prediction against a snapshot that had naturally moved forward on its own is not a misprediction, and was being misreported as one.
- Switched the one-cell prediction from the most recently queued turn to the oldest unacknowledged one. The server always applies queued turns in the order it received them, so predicting off the newest turn guessed a direction the server had not reached yet whenever two turns queued up before the first was acknowledged. This produced a diagonal, perpendicular-direction resync on the very next snapshot even though nothing was actually wrong, and was especially visible during ordinary rapid turning.
- Mirrored the server's wall-grace turn selection in the client predictor. When the oldest queued turn would carry the head out of bounds, the server already searches the remaining queued turns for the first one that stays on the board; the client had no equivalent logic and would predict the out-of-bounds turn the server was never going to use, producing the same kind of spurious diagonal resync, clustered specifically near the map edges. The client now performs the identical search.
- Fixed an unacknowledged input getting stuck at the front of the local input queue forever once it had exhausted its retries with no server acknowledgment, most likely from an occasional dropped WebSocket frame. Left in place, a single permanently stuck turn poisoned every future one-cell prediction and the food-eat prediction check, since both always read the oldest queued entry; this was the cause of a persistent stream of resyncs unrelated to walls or turning frequency, and of food not appearing to register an instant eat despite the server crediting it correctly. The client now drops an input once its retries are exhausted and falls back to trusting the authoritative body.
- Made the client's misprediction detection run regardless of whether the debug panel is open, rather than only while recording. The debug panel's own logging is unchanged and still only records while open; the detection itself now always runs because the correction glide effect below needs it available all the time.
- Disabled Nagle's algorithm on the server's WebSocket connections by setting TCP_NODELAY on the raw socket during the upgrade handshake. The server's frequent, small game-state broadcasts and direction messages could otherwise sit buffered by the kernel for tens of milliseconds waiting to be coalesced with more outbound data before being sent, adding avoidable round-trip latency for no benefit on a connection that is already small and frequent by nature. This does not address propagation delay or Cloudflare edge latency, only kernel-level buffering on the origin server itself.
- Added a clientFx block to config.json (inputFlash, inputFlashMs, correctionGlide, correctionGlideMs) served read-only to the client through a new GET /api/config endpoint, with hardcoded defaults on the server so the feature stays on even against an older config.json that predates the key. This is the only configuration surface for the two effects below: a single global on/off per effect for the operator, with no per-player setting and no client-side prompt.
- Added an input flash effect. The instant a keypress is accepted, a short fading strip is drawn on the leading edge of the player's own head cell in the pressed direction. It is timed purely by elapsed time since the keypress rather than by network ticks or by whether the body actually steps that tick, which is what makes it register as instant regardless of round-trip time.
- Added a correction glide effect. When a one-cell misprediction is corrected, the player's own head eases from the mispredicted cell to the corrected cell over a short configurable duration instead of snapping. This is a narrow, deliberate exception to the project's grid-snapped rendering; it applies only to the local player's own head cell during the correction window, and every other segment, every other player, and every other frame remain exactly grid-snapped as before.
- Considered and rejected moving to a hybrid authority model where the client reports its own position for moves the server can classify as uncontested (no food, no collision), with the server validating only contested moves. Rejected because it does not reduce latency, since the server still cannot approve a packet it has not received yet, contested or not, and it trades away the server's current full authority over position, which is what currently makes every leaderboard score impossible to fake, for a cheating surface that would need closing again per powerup once movement stops being simple axis-aligned steps. Server remains authoritative for all positions.

#### Couch co-op / dual local controls (round seven, Phase 3)
- Added a second local player per connection. A single WebSocket connection can now control up to config.maxLocalPlayers (default 2) slots at once: p1 on arrow keys as before, and an optional p2 on WASD, added on request via a new "+ Add Player 2" button and a joinLocal message. Each local player gets its own LocalPlayerPredictor instance; predict.js needed no changes since its per-instance state (input buffer, correction tracking, retry state) was already fully self-contained from the prior round.
- Replaced each connection's single slotIndex with an ordered slots array (index 0 = p1, index 1 = p2, ...) throughout server.js: assignment, disconnect cleanup, direction routing, and the you payload in every broadcast state now all key off this array instead of a lone integer.
- Direction and initials messages now carry a local index (0/1) so the server knows which of a connection's slots a turn or an initials submission applies to; both default to 0 when absent, so a solo connection that never sends the field behaves exactly as before.
- Decided, and documented in server.js, that a co-op connection's slot never yields to the spectator queue on death: since one human is already using their turn to occupy two slots, bumping only one of the pair out to spectate would strand that local player with no snake and no separate spectator UI of their own. A co-op slot always respawns immediately instead; a solo connection's slot still yields to a waiting spectator exactly as before. This policy is new and has not been confirmed against a live multi-client session; see TODO.md.
- Extended render.js's cosmetic fx plumbing (input flash, correction glide) and ui.js's DEBUG panel from single-player-shaped objects to per-local-index arrays, so p1 and p2's effects and pending-input/correction logs never overwrite each other.
- Verified the new connection/slot bookkeeping (solo join, co-op add, the co-op cap, disconnect freeing both slots, co-op-refused-behind-a-waiting-spectator, and the co-op-respawns-vs-solo-yields death policy) against a scratch headless harness mirroring the exact server.js logic; not committed, per the existing tests/ note in TODO.md.
- Did not touch the initials-queue / spectator-yield item already on TODO.md ("Verify the initials queue and spectator yield behavior end to end in a real browser with multiple clients"); that item still needs a live multi-client browser session and is unrelated to this round's slots-array refactor beyond the new co-op-respawn carve-out noted above.

#### Couch co-op fairness correction: strict round robin (round eight)
- Corrected a design mistake from round seven: a co-op connection's second local seat previously always respawned immediately on death, bypassing the spectator queue entirely regardless of who else was waiting. That let one connection occupy two board slots indefinitely while other people sat queued, which is exactly the kind of hogging the operator does not want -- every local seat needs to be round-robin fair on its own, with no advantage for co-op.
- Replaced the connection model again: connections no longer have a single role/slots pair, or even a single conn.slots array. Each connection now has a locals array indexed by local index, and each entry independently holds either { role: "player", slotIndex } or { role: "spectator", slotIndex: null }. A connection's two seats can be in different roles at the same time -- p1 playing while p2 sits in the spectator queue is normal, not an edge case.
- The spectator queue and join-offer mechanism now operate on (connId, localIndex) pairs instead of connId alone, so each local seat waits its own turn independently. admitLocal() is the single admission path for both a fresh connect and a joinLocal request: take a free slot immediately only if no one else is waiting, otherwise join the back of the same queue everyone else uses. joinLocal essentially never fails outright anymore (aside from the maxLocalPlayers cap) -- it either seats the new local player immediately or queues it fairly, exactly like a fresh connection would be treated.
- respawnOrSpectate() lost its co-op special case entirely: a slot yields to the spectator queue on death under the exact same rule regardless of whether it's a solo connection's only seat or one seat of a co-op pair. The connection's other seat, if any, is never affected by what happens to this one.
- The state broadcast's per-connection payload changed again: you.role/you.slots (round seven) is replaced by you.locals, an array of per-seat { role, slot, ack } or { role, queuePos, queueLen } entries. There is no longer a single top-level role for a connection, since the two seats can now genuinely differ.
- Client-side, this meant tracking curr.you.locals per local index in main.js instead of a single myRole.role, and reworking ui.js's initials/spectator/join-offer overlays to take an isCoOp flag: a genuine solo connection keeps the original full-screen prompt, but once co-op is active, either seat could need a prompt while the other keeps playing, so both switch to small non-blocking corner panels instead.
- One known simplification, not yet live-tested: if a queued co-op seat's idle-spectator timer expires while the connection's OTHER seat is still actively playing, that seat just quietly drops out of the queue rather than the whole connection being disconnected (which would incorrectly eject the still-playing seat too). Flagged in TODO.md.
- Re-verified the connection/seat bookkeeping against a new scratch headless harness (round seven's harness is now stale and was replaced, not extended, since the underlying model changed): solo + co-op admission, a co-op seat correctly queuing behind an already-waiting spectator instead of jumping ahead, strict first-in-line-wins ordering when a slot frees up, and a co-op seat's death yielding to the queue exactly like a solo seat's would. Not committed, per the existing tests/ note in TODO.md.

#### Couch co-op usability fixes (round nine)
- P2 (WASD) now joins automatically the first time a WASD key is pressed, not only via the "+ Add Player 2" button -- the button still works and stays in sync (it disables itself once co-op is confirmed by the server either way). The triggering keypress itself is the join request; it is intentionally not queued as a move, since there is no seat to queue one for until the server confirms it.
- Fixed a real problem with two people sharing one keyboard: if both local seats died and qualified for a high score around the same time, the server previously sent BOTH initials prompts at once, so two overlays and two countdowns competed for the same physical keyboard -- confusing at best, and whichever one someone clicked into effectively took the other's controls out of the loop while they typed. Server now serializes initials prompts per CONNECTION (not per seat): if one local seat's prompt is already live, a second qualifying death on the same connection is queued rather than shown immediately, and its countdown does not start until it is actually displayed (so it can never silently expire while hidden behind the first one). Two people playing from separate connections/computers are entirely unaffected -- each connection's own initials prompt is independent and was never queued against anyone else's.
- Verified the queuing logic (never two live prompts on one connection, the second prompt's deadline provably has not started while queued, and it gets a full fresh countdown once shown) against a new scratch headless harness mirroring handleDeath/startNextPendingInitials/the initials handler verbatim. Not committed, per the existing tests/ note in TODO.md.

#### Leave flow, high-score state machine, boost/slide, idle cleanup, latency pass (round ten)
- Added an explicit "Leave" flow. A new leaveLocal message removes ONE local seat completely: its board slot is freed (or its spectator-queue entry dropped), and the seat becomes a null hole -- it is deliberately NOT pushed into the spectator queue. Rejoining requires a fresh joinLocal, which now fills the leaver's old hole first so the key maps (0 = arrows, 1 = WASD) stay stable. Leaving the LAST seat on a connection tears the whole connection down and closes the socket: the client shows a menu/rejoin screen (Play reloads and re-runs the captcha gate) instead of idling on a dead board. The client renders one Leave button per existing seat, kept in sync every state broadcast.
- Replaced the per-slot initials prompt state (awaitInitials/initialsDeadline) with a connection-scoped high-score state machine: conn.pendingInitials (queue of { local, targets, score }) plus conn.activeInitials (the one prompt on screen, with its deadline). A qualifying score is BANKED on death, and also on leave if the seat left mid-game with a qualifying score. Prompts are never shown while any local seat on the same connection still has a living snake (shared-keyboard rule); once nothing is alive, they flush one at a time, each with a fresh countdown. A seat that dies while a flush is owed is parked as role "held" -- its slot is freed for round-robin fairness -- and every held seat is re-admitted through the normal admitLocal path once the queue drains, so P1 resumes solo play after entering initials. Leaving during your own live prompt counts as declining it and advances the flush.
- Hardened the initials handler while rewriting it: the server now records the score and boards it tracked for the active prompt and ignores the client-supplied score/targets fields, so a client can no longer submit a fabricated score to the leaderboards.
- Expanded the inactivity timeout to active multiplayer play: each slot tracks lastInputAt (updated by dir and boost messages), and when EVERY living snake has been silent for config playerIdleMs (default 120000), the sweep disconnects the idle connections like the spectator idle rule does. lastInputAt is deliberately not reset on automatic respawn, so an AFK player who keeps dying and respawning cannot keep resetting their own clock; one attentive player keeps the lobby alive for everyone.
- Made the build number real instead of a hand-edited string constant. server.js resolves its build at startup: commit count + short hash from git in a live checkout, else a build-info.json stamped by git archive export-subst (new .gitattributes entry) in release archives, else package.json version marked "dev". The result is broadcast in every state message and shown in the existing debug panel's server build line, so a stale deploy is immediately visible.
- Added a boost-with-inertia mechanic (config boost.enabled/boostSpeed/slideDistance, defaults on/1.5/2). Holding the key of the current travel direction boosts that snake: movement accumulators are now PER SNAKE, and a boosting snake's accumulator fills boostSpeed times faster against the shared ramped interval. The trade-off is the slide: a turn queued while boosting is tagged with delay = slideDistance and drifts that many further straight cells before registering (the wall-grace lookahead may still consume it early to avoid a wall death, intentionally). Collision stayed step-synchronous: movers are checked against EVERY living snake, with a non-mover's body (tail included, since it does not vacate) as a solid obstacle and head-on death only between two snakes moving the same step. Client-side, a held-key detector sends only boost on/off transitions, and turns typed while boosting are sent and acked normally but flagged so the predictor does not pre-play a turn whose landing tick only the server knows.
- Added operator-configurable zero-overhead debug (config enableDebug, default true). Server: a single dlog binding that is null when disabled, so every call site is one falsy short-circuit -- no strings built, nothing buffered. Client: /api/config now carries enableDebug, and when false the DEBUG button, panel, refresh timer, and correction recording are simply never constructed.
- Added client-side lock-step render smoothing (config clientRender.interpolate, default true, served via /api/config). The state broadcast now includes each player's effective ms-per-cell (moveMs, boost included), and render.js eases every server-rendered snake segment from its previous-snapshot cell to its current cell over exactly that interval, so travel is smooth and in step with the server cadence instead of jumping a whole cell per broadcast. Local predicted bodies keep the existing prediction/glide path; teleport-sized jumps (respawn, growth) snap. This reverses round four's "no sub-cell interpolation" decision at the operator's request, as a purely cosmetic client setting -- server collision and authority are untouched.
- Latency/perf pass over the hot paths: state broadcast now serializes the shared board once and splices each connection's small you payload into the JSON string (was a full re-stringify per connection); spectator queue positions are precomputed per broadcast instead of a findIndex per seat per connection; collision scans use index loops instead of slice().some() allocations; the sim loop schedules against an absolute next-tick time so timer jitter no longer accumulates into a slower effective tick rate; permessage-deflate is now explicitly disabled on the WebSocket server with the reasoning documented.
- Verified end to end against the live server with scripted WebSocket clients: boost rate change and measured cell speed, the 2-cell slide before a boosted turn, P2 leave/rejoin filling the same local index, solo leave closing the connection, no initials prompt during active play in both spec scenarios (P2-leaves-then-P1-dies and both-die), sequential flush with held-seat re-admission, leaderboard persistence of the flushed score, and the all-idle lobby cleanup (with a shortened playerIdleMs) including the one-active-player-keeps-lobby-alive case. Not committed, per the existing tests/ note in TODO.md.

#### Join-screen boost tip (round eleven)
- Added a brief boost/slide explainer to the captcha overlay (index.html #boostTip), visible while a new player is solving the captcha rather than after they are already moving. A "Got it" button dims it as a lightweight acknowledgement; clicking it does not gate the Join button, since it is purely informational and boost may be disabled server-side anyway. The client hides the tip entirely when /api/config reports boost.enabled: false, so players are never told about a mechanic that cannot fire on that server.
